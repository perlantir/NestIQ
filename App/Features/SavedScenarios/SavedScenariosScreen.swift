// SavedScenariosScreen.swift — Session 3.2 wires the real screen.

import SwiftUI

struct SavedScenariosScreen: View {
    var body: some View {
        Text("Saved — Session 3.2")
            .textStyle(Typography.h2)
            .foregroundStyle(Palette.ink)
    }
}
