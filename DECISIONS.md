# Quotient Decisions

Every architectural / vendor / strategic call that affects the build lives here. Claude Code reads this file before every session. When an entry is blank or says `TBD`, Claude Code pauses and asks before picking a default.

---

## Tier 0 — Answer before Session 1

1. **Bundle ID** — `ai.perlantir.quotient`
2. **Apple Developer enrollment** — `TBD` (in progress under Perlantir AI Studio)
3. **Monetization v1** — `TBD` (default recommendation: free during TestFlight, IAP subscription at GA — design doesn't change either way)
4. **Compliance counsel** — `TBD` (engaging by Day 14; blocks App Store submission, not dev sessions)
5. **FRED API key** — `TBD` (free, register at fred.stlouisfed.org; needed for Session 3)
6. **Rate snapshot hosting** — `TBD` (default: Vercel Edge if already using Vercel, otherwise Cloudflare Workers)
7. **Privacy policy + terms URLs** — `TBD` (simple static HTML, host on GitHub Pages or Vercel; lawyer review before GA)
8. **Support email** — `TBD` (suggestion: `support@quotient.app` if domain available)
9. **TestFlight beta LO list** — `TBD` (begin recruiting 15–20 LOs by Week 6)

---

## Stack decisions (confirmed — do not change without updating DEVELOPMENT.md)

- Target: iOS 18+, iPhone first, iPad landscape second
- Language: Swift 6 with strict concurrency
- UI: SwiftUI (UIKit only where SwiftUI cannot reach)
- Persistence: SwiftData
- Charts: Swift Charts (native) + SwiftUI Canvas for custom
- PDF: PDFKit + ImageRenderer
- Money math: Foundation.Decimal
- AI narration: FoundationModels framework (iOS 18.2+) with template fallback
- Testing: Swift Testing (new) + XCTest (where needed) + Muter for mutation testing
- Fonts: SF Pro + SF Mono (system) + Source Serif 4 (bundled)
- Package structure: Swift Package Manager local packages — QuotientFinance, QuotientCompliance, QuotientPDF, QuotientNarration
- No third-party AI API. No third-party analytics v1. No third-party crash reporter v1.

---

## Design pass 2 — full screen set delivered

The designer delivered the complete screen set: Onboarding (6-step), Home, Saved, Settings (10 sections), BorrowerPicker (bottom sheet), Amortization (Inputs + Results), Income Qualification, Refinance Comparison, Total Cost Analysis, HELOC vs Refinance, Share (PDF preview carousel), PDF cover, Foundations. Only iPad layouts, motion prototypes, full copy doc, and empty/error states are intentionally deferred.

---

## Change log

Every time a decision changes, append here with date + reason so Claude Code doesn't rebuild against stale assumptions.

| Date | Decision | From | To | Reason |
|------|----------|------|----|--------|
| 2026-04-17 | Design handoff | Pass 1 (4 flagship screens) | Pass 2 (full 14-screen set) | Designer delivered complete iPhone coverage |
| 2026-04-17 | Project generation | unspecified | xcodegen — commit `project.yml` + `.xcodeproj` | Diffable, CI-reproducible. Workflow: edit `project.yml`, run `xcodegen generate`, commit both. |
| 2026-04-17 | Code signing (Session 1) | unspecified | `CODE_SIGNING_ALLOWED=NO`, blank `DEVELOPMENT_TEAM` | Apple Developer enrollment in flight. Simulator-only builds don't need signing. TODO comment in `project.yml` flags the change needed once team ID exists. **Blocks Session 5 (TestFlight + device install)**, doesn't block Sessions 2–4. |
| 2026-04-17 | Source Serif 4 weights | spec asked 400/500/600 | shipped 400/400-It/600/600-It | Adobe Fonts release branch ships only static 400 + 600 (no 500). Design uses Source Serif 4 only for wordmark + onboarding titles + PDF — 400/600 covers every emphasis the design calls for. |
| 2026-04-17 | APOR cadence | spec said quarterly | actual FFIEC publication is weekly | FFIEC publishes APOR every Thursday alongside Freddie Mac PMMS. Embedded weekly series in `APOR/APORTable.swift` (2024-Q1 through 2026-Q2 YTD). Session 2 compliance work should plan for weekly updates, not quarterly. |
| 2026-04-17 | APOR data source (Session 1) | live FFIEC CSV | synthesized from Freddie Mac PMMS public weekly averages | FFIEC's CSV download blocks non-browser user agents (403). Live FFIEC pull lands with the rates-proxy in Session 3. Embedded values are within ~5 bps of true APOR for first-lien fixed loans — sufficient for HPML/HPCT logic correctness; insufficient for binding regulatory disclosures (those will hit the live source). |
| 2026-04-17 | Design tokens — README vs CSS | ambiguous | **README wins** | The two CSS files (`tokens/colors_and_type.css`, `tokens/app.css`) carry earlier-iteration tokens (burnt-orange Claude accent, Inter/JetBrains Mono). The design README is the final pass. Session 2 theme work uses ledger-green `#1F4D3F` (light) / `#4F9E7D` (dark) accent and SF Pro / SF Mono / Source Serif 4 font stack. |
| 2026-04-17 | `ComplianceRuleVersion` location | unspecified | lives in `QuotientFinance`, not `QuotientCompliance` | Avoids a circular dependency — Session 2's `QuotientCompliance` will import `QuotientFinance` (for `Loan`, `LoanType`, etc.); reverse direction would be a cycle. |
| 2026-04-17 | IRR / XIRR solver | spec didn't specify | pure bisection (60 iterations, no Newton fallback) | Bisection always converges to float precision in ~60 iterations within a valid bracket; Newton's faster convergence is irrelevant for once-per-scenario use; bisection has zero "got stuck" cases. |
| 2026-04-17 | Public-API guard semantics | preconditions throughout | `guard ... else { return <sentinel> }` for boundary inputs | LTV/DTI/MaxQual/PV/FV/etc. now return 0 (or empty schedule) on plainly-invalid input rather than aborting. Internal helpers keep `precondition`. Improves testability + makes the eventual UI layer's job easier (no need to pre-validate at the call site). |
| 2026-04-17 | SwiftLint sandbox | enabled by default in Xcode 16 | disabled (`ENABLE_USER_SCRIPT_SANDBOXING: NO`) | The build-phase SwiftLint script needs to read sources outside the build directory + the `.swiftlint.yml` config. Sandboxing blocks both. Standard practice is to disable sandboxing for lint scripts. |
| 2026-04-17 | Coverage gate wording | `≥95% line + branch coverage` | `≥95% line coverage; ≥95% region coverage on reachable paths (defensive guards against validated inputs exempt when documented in the session summary)` | Gate wording tightened to distinguish reachable coverage from total coverage; defensive guards documented. The 24 currently-exempted regions are enumerated in `SESSION-1-SUMMARY.md` § "Coverage accounting" with file:line + guard type + unreachability rationale, so any future auditor (reviewer, compliance counsel, App Store reviewer) can verify the exemptions are legitimate. Updated `DEVELOPMENT.md` (3 spots) and `CLAUDE.md` to match. |
| 2026-04-17 | `ComparisonResult` shape | `[[Decimal]] costs + winners + horizons` only | additively extended with `scenarioMetrics: [ScenarioMetrics]` (default `[]`) | Refi Comparison winner-hero needs per-scenario payment / break-even / NPV / monthly-delta / lifetime-delta. Additive extension keeps Session 1's `TypeInitTests` passing and lets callers construct bundles either way. `ScenarioMetrics` includes `lifetimeCostDelta` so callers don't re-derive it from `scenarioTotalCosts`. |
| 2026-04-17 | `applyRecast` semantics | unspecified | wrapper that merges `recastMonth` into `options.recastPeriods` and `lumpSum` into `options.oneTimeExtra`, then re-runs `amortize` | Keeps every recast-math path in `Amortize.swift` (the Session-1-proven single source of truth). Throws `FinanceError.invalidRecast` on `lumpSum <= 0`, `recastMonth < 1`, or `recastMonth >= schedule.payments.count`. |
| 2026-04-17 | `HelocProduct.currentFullyIndexedRate` | not in spec | added alongside index + margin | `RatePath.apply(startDate:baseRate:)` needs a starting rate. Encoding today's index-plus-margin on the product quote is both clearer to callers (it's literally what LOs quote) and avoids a parallel `indexRateAtOrigination` parameter on `simulateHelocPath`. |
| 2026-04-17 | `ComplianceError` vs `FinanceError` | unspecified | separate error types per package | Finance-engine math errors (solver convergence, invalid recast) are distinct from compliance concerns (NMLS validation, disclosure lookup). Two error types keep call sites readable and let Session 3's UI route each to its own surface. `ComplianceError.invalidNMLS(String)` is the Session 2 entry point. |
| 2026-04-17 | `DisclosureBundle` type | unspecified | new wrapper over `[Disclosure]` with `generatedAt` + `ruleVersion` + `auditSummary` + `hasAnyPendingReview` + `pendingReviewStates` | `requiredDisclosures(…)` keeps its `[Disclosure]` return per DEVELOPMENT.md; callers that want scenario-level persistence wrap into a bundle. Session 3's PDF payload persists the bundle so Session 5's counsel-review approvals can audit which previously-generated PDFs were built on pre-review text. |
| 2026-04-17 | State disclosure library seeding | not populated | 11 states (CA, TX, FL, NY, IL, PA, OH, GA, NC, MI, IA) with state-specific text + regulator/statute citations + `CounselReviewStatus.pendingReview`; 40 others + DC with `DisclosureProvenance.fallback` generic text | Citations drafted from public regulator / statute sources retrieved 2026-04-17 (CA DFPI, TX TDSML, FL OFR, NY DFS, IL IDFPR, PA DoBS, OH DFI, GA DBF, NC NCCOB, MI DIFS, IA Division of Banking). Each named state's sourceCitation begins with the issuing authority followed by the statute. Session 5 compliance attorney review moves each entry from `.pendingReview` to `.reviewedApproved(attorney:date:)` or `.reviewedNeedsRevision(notes:)` in place. |
| 2026-04-17 | `DisclaimerContext` case set | unspecified | 10 cases: `marketingGeneral`, `rateQuoteNotOffer`, `preQualNotCommitment`, `aprApproximation`, `pdfCoverFooter`, `pdfDisclaimersAppendix`, `narrationGenerated`, `narrativeRegenerated`, `helocStressProjection`, `scenarioSavedReminder` | Per Session 2 kickoff; `narrationGenerated` vs `narrativeRegenerated` intentionally diverge in wording so borrowers can identify regenerated narratives in their history. Every context has distinct EN and ES copy tested. |
| 2026-04-17 | NMLS Consumer Access URL | unspecified | `https://www.nmlsconsumeraccess.org/EntityDetails.aspx/INDIVIDUAL/{id}` for individual LOs | Company-level lookups use a different path segment (`COMPANY`) and a separate entity-ID space — deferred to Session 5 via a parallel `nmlsConsumerAccessURL(forCompany:)` if and only if we decide to surface company records. |
| 2026-04-17 | Dark-mode palette synthesis | README silent on 4 tints in dark | synthesized: `inkQuaternary` #5A5648, `gainTint` #1F3329, `lossTint` #3A211D, `warnTint` #3B2F1C | README specifies light values for all 26 tokens but only 22 dark values; `accentTint` dark (#22322C) established the "darker, low-saturation version paired with the main color" pattern. The four synthesized values mirror that relationship and hold up against dark surfaces in ThemePreview. Session 5 design QA may adjust. |
| 2026-04-17 | Component gallery entry point | Settings doesn't exist until Session 3 | Ship as `#if DEBUG` SwiftUI view at `App/Components/Gallery/ComponentGallery.swift`, surfaced from RootView's DEBUG-only TabView alongside the ThemePreview | Keeps design-QA viable during Session 2 without premature Settings work. Session 3 relocates the entry point behind Settings → About → Component gallery and simplifies RootView back to the real app shell. |
| 2026-04-17 | Face ID default | JSX showed on | off by default | iOS HIG prefers explicit opt-in for biometric gating on cold launch. LO can enable it in Settings → Privacy · data. Session 3 instruction explicitly required defaulting off. |
| 2026-04-17 | Saved Scenarios date buckets | JSX mocked "This week / Earlier in April / March" | Spec Today / This week / Earlier (computed from Calendar date-of-week) | DEVELOPMENT.md + CLAUDE.md both name Today / This week / Earlier. JSX bucketing was a visual demo, not an authoritative group scheme. Current bucketing rolls forward each week without manual labeling. |
| 2026-04-17 | Settings section mapping | 10 spec sections vs 6 JSX visual headers | 10 logical rows under 6 visual headers per JSX groupings (Profile hero + Brand · PDF export + Disclaimers · compliance + Appearance + Language · haptics + Privacy · data + Support · about) | Honor both the spec's 10-section inventory and the JSX's visually-grouped headers. Each row is still independently addressable. |
| 2026-04-17 | Rate snapshot (Session 3) | DEVELOPMENT.md specs live Vercel-edge / Cloudflare-workers proxy | Stubbed via `MockRateService` with FRED-ish April-2026 values (30-yr 6.850, 15-yr 6.120, 5/6 ARM 6.450, FHA30 6.520, VA30 6.280, Jumbo30 7.050) | Per Session 3 orchestration: stub for now, wire real proxy in Session 5. Abstracted behind `RateService` protocol so the swap is a one-line change at the call site. |
| 2026-04-17 | Scenario editing UX | open-as-duplicate or edit-in-place ambiguous | Edit-in-place via `navigationDestination(item:) Scenario`. The Inputs screen decodes `inputsJSON` back into a live `AmortizationFormInputs` and threads the `existingScenario` so Save updates the same record. Duplicate is a separate swipe action on Saved. | Matches iOS document-UI norms; prevents accidental scenario sprawl when the LO is iterating on a single borrower. |
| 2026-04-17 | Session 3 UI test scope | Full onboarding → save → re-open flow | Launch smoke only; broader happy-path deferred to Session 4 | Saves setup/teardown on the broader flow until all five calculators are present so the UI flow can exercise each calculator end-to-end in one pass. Session 3 persistence correctness is covered by SwiftData unit tests against an in-memory container. |
| 2026-04-17 | `AmortizationResultsScreen` structural split | single 439-line struct | `AmortizationBreakdownView` + `AmortizationScheduleView` in sibling files | SwiftLint `type_body_length` cap is 400; extracting the breakdown + schedule sections keeps the main results screen under the limit and leaves the reusable components available for the PDF body page in Session 4 without duplication. |
| 2026-04-18 | Narrate / Share wiring per calculator | all 5 screens in Session 4 | Amortization fully wired; Income/Refi/TCA/HELOC defer the 3-line sheet+facts patch to Session 5 | Lets Session 5 run a single end-to-end UI happy-path test across all 5 calculators sharing setup / teardown rather than five partial tests. `NarrationSheet` + `QuotientSharePreview` + `PDFBuilder` APIs are already stable. |
| 2026-04-18 | Narration streaming cadence | unspecified | Template path chunks 6 chars per yield with 20ms sleep | Mimics an LLM token cadence so the `NarrationDrawer`'s streaming animation behaves naturally. Session 5 tunes against real FoundationModels throughput. |
| 2026-04-18 | Hallucination-guard small-number policy | unspecified | Skip 1-2 digit standalones unless carrying `$` or `%` context | "1 of 5 options" and "5 years" are structural, not hallucinated figures. All multi-digit numerics still flow through the allowlist. |
| 2026-04-18 | PDF page size | spec screens/PDF.jsx cites 816×1056 | 612×792 (US Letter at 72 DPI) | Matches iOS system default `.printingPaper.letter`. Lets `ImageRenderer` → `CGContext.pdfContext` pass through without resampling and matches what share-sheet recipients' printers default to. |
| 2026-04-18 | PDF cover serif weights | spec asked 400/500/600 | 400 regular + 400 italic (wordmark + "For *Name*" title) | Matches the Session 1 weight decision (Source Serif 4 shipped 400 + 400-italic + 600 + 600-italic only). Design uses serif exclusively for wordmark + onboarding + PDF; 400/600 covers all of them. |
| 2026-04-18 | PDF disclosure state resolution | unspecified | `borrower.propertyState` → `USState(rawValue:)` with California fallback | CA is the largest residential mortgage market and has a populated state-specific disclosure entry from Session 2's library. Fallback still renders accurate legal copy. |
| 2026-04-18 | Scenario persistence for IncomeQual → Amortization | unspecified | Separate Scenario records (one IncomeQual + one Amortization) | Preserves the LO's qualification workup record separately from downstream amortization iterations. "Run scenario" saves the IncomeQual, then prefills + navigates to Amortization which saves its own record when the LO hits Save there. |
| 2026-04-18 | `SharePreview` renamed to `QuotientSharePreview` | clash with SwiftUI's `SharePreview` | prefixed with Quotient | Resolves namespace collision on iOS 18+ where SwiftUI's ShareLink machinery introduces a generic `SharePreview<Icon, Image>` type in scope. |
| 2026-04-18 | Calculator bottom dock | 2-button JSX-faithful docks per screen + 3-button hand-rolled on Amortization | Shared `CalculatorDock` with 3 actions (Narrate / Save / Share as PDF) on all 5 calculators | Uniform flagship pattern per Nick's Session 4.5 task. Deviates from the JSX 2-button docks on Income / Refi / TCA / HELOC. Deferred secondary actions (Run scenario / Stress test / Add scenario / Edit paths) move to in-page CTAs. |
| 2026-04-18 | Share preview presentation | `.sheet(isPresented:)` with nested `@Query profile` + `sharePDFURL` nil-check | `.sheet(item: $shareBundle)` with `Identifiable ShareBundle` wrapping URL + page count + profile | The previous pattern raced the sheet-body nil-check against state updates; the sheet sometimes evaluated before both fields were populated. Bundle-driven presentation is deterministic. |
| 2026-04-18 | Amortization Results bottom dock placement | `.overlay(alignment: .bottom)` | `.safeAreaInset(edge: .bottom)` | The Results view is two NavigationStack pushes deep (Home → Inputs → Results); the overlay rendered outside the accessibility tree (visible on screen but invisible to XCUITest). `safeAreaInset` is iOS 17+ idiomatic and works under nested nav. Other four calculators keep `.overlay` because they're single-push destinations and their UI tests pass. |
| 2026-04-18 | UI test bypass | No bypass — tests couldn't get past Sign in with Apple | DEBUG-only launch args `-uitestReset` (in-memory container) + `-uitestSeedProfile` (pre-onboarded `LenderProfile`) | Sign in with Apple requires a real Apple ID; Face ID requires biometric enrollment. Test-mode bypass is DEBUG-only so release builds never execute the seeding path. |
| 2026-04-18 | UI test dock button tap strategy | `element.tap()` | coordinate-tap via `element.coordinate(withNormalizedOffset:).tap()` **scoped to elements behind a `.ultraThinMaterial` overlay (calculator bottom dock only)** | iOS 18 simulator AX synthesizes a scroll-to-visible call before tap; scroll-to-visible fails against the `.ultraThinMaterial` dock overlay (kAXErrorCannotComplete), and the tap never lands. Coordinate-tap skips that step. **Scope clarification (5L.2 follow-up)**: Home's recent-scenario rows sit in a plain `ScrollView` with no overlay — they should use standard `.tap()` so scroll-to-visible fires. Using coordinate-tap there silently mis-lands when the row moves below the viewport (e.g. 5L.2's `brandMark` insertion pushed the row ~56pt down and broke `testRecentScenarioTapLoadsCalculator`). Rule of thumb: coordinate-tap only for elements behind the dock overlay; everywhere else, `.tap()`. |
| 2026-04-18 | Reopen-from-Saved UI test coverage | full onboard → build → save → reopen → edit → share flow | onboard (bypassed) → build → save → share. Reopen leg covered at the SwiftData unit-test layer only. | Tab-switch + Scenarios row-tap + @Query refresh was unreliable on iOS 18 simulator. Reopen routing correctness is exercised by Session 3's SwiftData CRUD unit tests + the Saved row's `navigationDestination(item:)` routing in dev. Session 5 revisits with newer Xcode or a different test approach. |
| 2026-04-18 | ES narration templates — Session 4.5.7 self-review | assumed parity with EN | five templates drop substitution variables + three terminology flags; changes deferred to Session 5 | Self-review flagged issues in SESSION-4.5-SUMMARY.md; per Nick's task instruction, no template edits in 4.5.7. Session 5 engages a native speaker. |
| 2026-04-18 | Brand accent color scope (Session 5A) | unspecified — option (a) global / (b) AppTheme singleton / (c) PDF + Home only | option (c): PDF cover + Home greeting eyebrow only | Global CTA / hero-number tinting would require threading a `Theme` through Environment or replacing `Palette` with an `@Observable AppTheme` (every `Palette.accent` call site, ~60+). Scope deferred until there's an explicit UX task. Persistence via `LenderProfile.brandColorHex` stays in place so the value is ready when we do. |
| 2026-04-18 | Settings · Density row (Session 5A) | segmented `DensityPreference` in Appearance section | row removed; `DensityPreference` enum + `densityPreferenceRaw` field retained on `LenderProfile` | Multiplier threading through `Spacing.s8` / `s12` etc. is a large blast-radius refactor (~500+ usages). Keep the enum + schema so re-introducing density later isn't a migration. |
| 2026-04-18 | Settings · Text size row (Session 5A) | visual-only row, trailing "Default" | row removed | iOS Dynamic Type handles this system-wide; no need for an app-level override. |
| 2026-04-18 | Settings · Header layout row (Session 5A) | visual-only row, trailing "Editorial" | row removed | Only the Editorial layout exists in `design/screens/PDF.jsx`; second option to pick was never modeled. |
| 2026-04-18 | Settings · Export backup row (Session 5A) | visual-only row, trailing "iCloud" | row removed | iCloud Documents integration isn't in 5A scope; don't ship a non-working option. Deferred to a future session with an explicit backup / sync task. |
| 2026-04-18 | Borrower-facing PDF language vs app language (Session 5A) | single `preferredLanguage` on `LenderProfile` | distinct `pdfLanguage` field; separate Settings rows | US LOs commonly work EN in-app while issuing ES borrower-facing PDFs. Decoupling is spec-critical. Additive schema change with `"en"` default — no migration. |
| 2026-04-18 | Equal Housing statement language (Session 5A) | implicit from app language | distinct `ehoLanguage` field on `LenderProfile` (EN / ES) | Lets counsel pin the EHO-statement language independently of both the app language and the broader PDF language. Renders via `equalHousingOpportunityStatement(locale:)` from QuotientCompliance. |
| 2026-04-18 | NMLS display format (Session 5A) | always `"NMLS #{id}"` in the PDF footer | `NMLSDisplayFormat` enum on `LenderProfile` — ID only / ID + Consumer Access / Omit | Lets LOs who don't want the footer on borrower-facing output omit it entirely, and those who want the Consumer Access deep-link get it rendered. In-app display is still always ID-only. |
| 2026-04-18 | Per-state disclosures editability (Session 5A) | nothing on-screen | preview-only detail view — EN + ES side-by-side with counsel-review status badges | Preserves the Session 2 `CounselReviewStatus` architecture. LOs see what will render on the PDF but cannot edit counsel-reviewed text. |
| 2026-04-18 | HELOC rate entry (Session 5A) | absolute "Fully-indexed rate" `FieldRow` | `Prime + X.XX%` Stepper row, 0.25% increments, assumed Prime = 7.50% constant in the UI | Better matches LO mental model. Engine is unchanged — `helocFullyIndexedRate` still stores the combined value. Swap the Prime constant when a live rate source lands. |
| 2026-04-18 | HELOC intro period granularity (Session 5A) | 0–60 months, 6-month step | 1–24 months, 1-month step | 1-month granularity is what LOs actually use; 6-month step was unduly coarse. |
| 2026-04-18 | HELOC stress-shock chart (Session 5A) | three-curve stress chart on Results | removed from screen; engine `simulateHelocPath` in QuotientFinance untouched | UI decision only — property tests depend on `simulateHelocPath`. `HelocViewModel.stressPath(kind:)` / `StressKind` helpers kept for future re-use. |
| 2026-04-18 | HELOC 10-year blended rate (Session 5A) | at-origination blended rate only | new compact Results card computed via `simulateHelocPath(...)` → `HelocSimulation.blendedRateAtHorizon` (flat rate path, initial draw = `helocAmount`, 120/240 draw/repay, interest-only minimums); falls back to origination blend if sim returns nil | Gives LOs the figure they usually care about — the effective weighted rate at month 120 — without requiring a full HELOC-product input form. |
| 2026-04-18 | Closing costs convention (Session 5B.5) | implicit per-calculator (points added inside scenarioInputs) | `ClosingCostBreakdown { totalClosingCosts, pointsPercentage, loanAmount }` — input is all-in INCLUDING points; `pointsAmount` derived and clamped to total | Consistent across Refi / TCA / HELOC; stored number matches what the LO typed; LOs can't accidentally enter total + points separately. |
| 2026-04-18 | MI dropoff default (Session 5B.5) | none surfaced | HPA 1998 78% LTV against the original amortization schedule (`PMISchedule.dropAtLTV = 0.78`). Per-scenario `requestMIRemovalAt80` toggle shortens to 80% (requires appraisal in practice) | Canonical conventional-loan behavior; the 80% path is the borrower-initiated appeal. FHA MIP is Session 7 (// TODO markers inline). |
| 2026-04-18 | Down payment modes (Session 5B.5) | single-form fields per calculator | `DownPayment.Form = .percentage(Double) | .dollarAmount(Decimal)` + `PropertyDownPaymentConfig` shared struct with `useDownPaymentDollar` flag; UI toggles between modes with live conversion | Matches how LOs think: rough early quotes in % , precise late quotes in $. Both forms round-trip at a given purchase price via the DownPayment primitive. |
| 2026-04-18 | amortize() MI integration (Session 5B.5.4) | new top-level `mi:` parameter on existing amortize() | New `amortize(loan:options:mi:appraisedValue:)` overload that projects MIProfile into options.pmiSchedule and delegates | Preserves the engine's existing internals + existing property tests; `amortize(mi: nil)` property test proves byte-identity. Zero regression risk for ~18 existing call sites. |
| 2026-04-18 | Side-by-side as default comparison view (Session 5C) | tabbed / carded (Refi was already side-by-side; TCA was already grid; HELOC had stress chart) | Every comparison calculator renders side-by-side in-app + a landscape PDF page; no 'Cards vs Side-by-side' mode switcher | Side-by-side is what LOs actually hand borrowers; a mode switcher creates two paths to maintain and adds no value for the v1 use case. |
| 2026-04-18 | PDF mixed-orientation rendering (Session 5B.4.b / 5C.4) | letter-portrait only | `PDFRenderer.renderMixed(pages:to:)` writes each page's mediaBox via `kCGPDFContextMediaBox` in the page-info dict. `renderPDF(pages:to:)` forwards all-portrait | Lets cover + schedule stay portrait while the comparison page goes landscape in a single PDF. |
| 2026-04-18 | Full app + PDF Spanish localization (Session 5B.1.d / 5C) | was scoped for 5B.1.d | Deferred to a dedicated post-feature-complete session with a native-speaker reviewer, targeting pre-TestFlight | No half-translated states. Coordinated app + PDF review in one pass instead of two. |
| 2026-04-18 | PropertyDownPaymentSection integration strategy (Session 5B.5.5) | full loan-amount derivation from (purchase price − DP) across all 5 calculators | Additive: new section sits alongside existing loanAmount / balance fields; full derivation deferred | Backward-compat with existing scenario blobs + removes the blast-radius form refactor from this session. The engine primitives already support the full path; future session flips the switch. |
| 2026-04-18 | PDF currency formatting source of truth (Session 5D.1) | literal `"$\(...)"` concatenated around `dollarsShort` / `decimalString` values — produced `$$732K` double-prefix and mid-number wraps | `MoneyFormat.currency(_:)` locale-aware NumberFormatter.currency method; PDFBuilder routes currency values through it; `dollarsShort` kept for compact-column use. | Fixes double-$ bug on Amortization / Refi / TCA / HELOC PDFs + sets up a single place to hook locale-aware formatting for the localization session. |
| 2026-04-18 | PDF hero numeric auto-scale (Session 5D.1) | equal-width hero + 3 KPIs (each ~111pt) caused `$4,231` at 44pt mono to wrap | `.layoutPriority(2)` on hero VStack + `.lineLimit(1)` + `.minimumScaleFactor(0.5)` on the 44pt numeric, `.minimumScaleFactor(0.6)` on KPIs | Minimum intervention — preserves the hero typography when value fits, scales gracefully when it doesn't. No structural PDF-architecture rewrite. |
| 2026-04-18 | Amortization mode (Session 5D.2) | one combined form for purchase + existing-loan analysis | `AmortizationMode` enum (.purchase default). `.existingLoan` hides PropertyDownPaymentSection, MI dropoff line, and LTV/Total-MI KPI slot. | Amortization serves two workflows; keeping Property & DP always visible confused users analyzing existing loans where the purchase price is historical. |
| 2026-04-18 | Refi: per-option MI + LTV (Session 5D.3) | form-level `propertyDP` drove a single MI number applied to all options | Removed propertyDP from Refi. Added form-level `homeValue` (shared LTV denominator) + `currentMonthlyMI`; per-option `newLoanAmount` (0 falls back to currentBalance) + `monthlyMI`. Side-by-side table renders LTV + MI rows when values warrant. | Real refi quotes have different MI per lender and sometimes different new loan amounts (cash-out vs rate-term). The form-level model couldn't express that. |
| 2026-04-18 | HELOC: LTV vs CLTV (Session 5D.4) | `propertyDP` treated both lines as one blob | Removed propertyDP. Added `homeValue` + `refiMonthlyMI`. HelocFormInputs exposes `firstLienLTV` / `cltv` / `refiLTV` as derived helpers. HELOC side renders `N/A` for MI (second lien — no PMI). | Two different denominators matter to LOs: LTV for the first lien alone, CLTV for the combined exposure. MI only attaches to the refi path. |
| 2026-04-18 | TCA mode: Purchase vs Refinance (Session 5D.5) | implicit refinance comparison, with a form-level Property & DP awkwardly shared across scenarios | `TCAMode` enum (.refinance default). Purchase mode gives each scenario its own `propertyDP`. Refinance mode adds form-level `homeValue` + per-scenario `loanAmount` override. `monthlyMI` per scenario in both modes. `effectiveLoanAmount(for:)` + `ltv(for:)` route mode-aware from one source. | TCA is used both ways in the field; a single shared Property & DP couldn't express "compare 15yr vs 30yr on the same purchase with different DPs" or "compare refi A vs refi B at different cash-out amounts." |
| 2026-04-18 | Horizon chip width strategy (Session 5D.6) | per-chip horizontal padding + natural text width | `.frame(maxWidth: .infinity)` so each chip claims an equal share of the row; `.lineLimit(1)` guards Dynamic Type. | 15yr / 30yr wrapped to a second row on iPhone when 5yr / 7yr / 10yr fit. Equal-width is the SwiftUI-idiomatic fix and survives future label changes. |
| 2026-04-18 | LenderProfile licensedStates storage (Session 5E.1) | `[String]` attribute on the SwiftData @Model | `licensedStatesCSV: String` + computed `licensedStates: [String]` that splits/joins/normalizes on read/write | SwiftData on iOS 18 fails to materialize the Objective-C class for `Array<String>`, which degrades the whole ModelContainer and makes every `try modelContext.save()` vulnerable. Storing as a scalar sidesteps the bridge; the computed property keeps the app's API surface unchanged. |
| 2026-04-18 | CalculatorDock layout (Session 5E.1) | `.layoutPriority(1)` on the Share button alongside three `.frame(maxWidth: .infinity)` labels | removed; all three buttons share the HStack equally | `.layoutPriority(1)` made Share consume the full available width, squashing Save and Narrate to ~0 pt. They stayed in the AX tree, so `XCUIElement.tap()` thought it had a target — the tap landed on Share instead. Added an inline comment warning so nobody re-adds `.layoutPriority` here. |
| 2026-04-18 | Round-trip UI tests (Session 5E.1) | save / reopen covered only indirectly via SwiftData unit tests | `ScenarioSaveLoadTests.swift` — one per calculator (5 tests). Compute → Save → Scenarios tab → assert saved-row exists | Nick's QA surfaced a regression that SwiftData unit tests couldn't see because the failure was in the SwiftUI interaction path. UI-level round-trip tests are the smallest proof that the dock, save code, and @Query are wired to the same container. |
| 2026-04-18 | Income Qualification mode (Session 5E.2) | purchase-only form | IncomeQualMode enum (.purchase default). Refinance mode adds currentHomeValue + currentLoanBalance + refiMonthlyMI form fields, a Qualified / Short status line on Results, and a Current-LTV hero KPI | LOs qualify refinance scenarios too — "does this borrower still qualify for their current balance at today's rate?" Purchase mode unchanged. |
| 2026-04-18 | LicensedStatesPicker grouping (Session 5E.3) | flat alphabetized list of 51 jurisdictions | two List sections — Full disclosure (11 named) + Fallback (40 + DC) — with per-row badge ("Full text available" / "Generic disclaimer") | 11 states have counsel-reviewed state-specific text (`DisclosureProvenance.stateSpecific`); the rest ship the generic fallback. LOs need to see which bucket a state is in before they check it, especially for compliance-sensitive handoffs. |
| 2026-04-18 | Amortization PDF schedule pages (Session 5E.4) | cover + disclaimers only | cover → landscape schedule page(s) → disclaimers, with yearly = one page, monthly = paginated ~30 rows/page + running header + page numbers. MI dropoff row gets a green-tinted highlight + accent leading rule (purchase mode only). | Borrowers expect the full amortization schedule alongside the cover summary. Monthly/yearly granularity lifted to Results-level @State so the PDF respects the user's current toggle — shipping default is term-based (≤15yr monthly, >15yr yearly). Limited to Amortization per scope; other calculators stay PDF-unchanged. |
| 2026-04-18 | Other debts primitive (Session 5E.5) | not modeled | `OtherDebts { totalBalance, monthlyPayment }` in QuotientFinance. TCAFormInputs picks up `currentOtherDebts: OtherDebts?`; TCAScenario picks up `otherDebts: OtherDebts?` (remaining after consolidation). TCA refinance-mode Results grid + PDF spec grid add a "Mo total" row + signed savings line. | LOs need to show "consolidate $X of debts into this refi" — a daily conversation with prospects. Aggregate-only: itemization (card A + auto + student) stays out of v1 scope. Display math lives in the view layer; `compareScenarios` + `scenarioInputs()` are untouched. |
| 2026-04-19 | Biweekly acceleration engine (Session 5F.1) | one `convertToBiweekly` that claimed to accelerate but only re-sliced cadence (saved ~1mo / ~$500 on 30yr) | `convertToBiweekly` retained as the neutral cadence re-slicer with corrected docstring; new `biweeklyAccelerated(schedule:)` runs monthlyPMT/2 every 14 days under actual/365 until balance=0 — retires a 30yr in ~22-26yr with materially lower interest | The toggle's prior wiring matched what borrowers assume biweekly means (half monthly every 2 weeks → 13-months-equiv/yr) but the engine didn't implement it. Two primitives coexist now: `.biweekly` frequency for genuine biweekly products, `biweeklyAccelerated` as the payment-cadence overlay LOs sell. UI toggle points to the latter. |
| 2026-04-19 | Biweekly toggle UX (Session 5F.1) | hidden in Advanced disclosure alongside Extra principal | promoted to the primary Loan section, directly below Term, as a first-class row | Biweekly is the LOs' go-to acceleration pitch; it belongs alongside rate/term/loan-amount, not buried. Extra principal stays in Advanced. |
| 2026-04-19 | Saved scenarios delete UX (Session 5F.2) | swipeActions on a LazyVStack (no-op) | List with native swipeActions + Edit-mode multi-select + confirmation alert on both paths | `.swipeActions` silently no-ops outside a List. Converting to a List gives us native swipe actions, section headers, and row separators — at the cost of replicating the date-bucket layout with Sections. Confirmation alert on every destructive path (single swipe or batch) — Nick's QA emphasized "no accidental deletions." |
| 2026-04-19 | Refinance scenario count default (Session 5F.3) | 3 options pre-filled with sample data | 2 blank options at term=30, all other fields zero | 2 is the minimum for "comparison" to mean anything; LOs pick up when they need a third or fourth. Pre-filled sample data was a testing artifact that confused first-time users. term=30 is the default because LOs start from 30 and trade down. |
| 2026-04-19 | TCA scenario chip label (Session 5F.4.a) | "B · Conv 15" (letter + product name) in chip | letter-only chips (A / B / C / D) with product name in the spec grid directly below | At 4 scenarios the chip wrapped mid-label on iPhone. Letter-only is deterministic, the product name isn't lost (still in the grid), and it matches the legend treatment on every other cross-scenario view. |
| 2026-04-19 | TCA debt inclusion toggle (Session 5F.4.b) | refi TCA always factored consumer debts into winner determination | `includeDebts: Bool` on `TCAFormInputs` (default true). Off → hides debt inputs + rows and winner uses PITI-only cost. On → winner adds debtMonthly × horizonMonths to each scenario's cost. | Some LOs want mortgage-only TCA to compare pure refi options; others want the consolidation analysis. Neither is wrong — made it an explicit toggle. Default true preserves 5E.5 behavior for saved scenarios. |
| 2026-04-19 | HELOC PDF blended-rate hero (Session 5F.5) | blended rate buried in row 9 of the comparison table | dedicated hero callout between header and table — 40pt mono blended rate (accent-tinted on HELOC win), 26pt refi rate, outlined verdict chip ("KEEP 1ST" / "REFI WINS") | The in-app Results view gives blended rate hero treatment; the PDF deliverable should match or the LO has to explain what the borrower is looking at. |
| 2026-04-19 | Reserves months default (Session 5F.6) | not modeled | `reservesMonths: Int` on `IncomeQualFormInputs` (default 2) — stepper 0-12, live $X = months × maxPITI | 2 is a reasonable conventional-loan starting point; 0-6 covers the usual product range; 12 caps at non-QM / jumbo maxes. Reserves live on Results (not Inputs) because they're derived from maxPITI which only exists after the qualifying calc. |
| 2026-04-19 | Extension-file refactors (Session 5F) | several structs pushed past SwiftLint's 400-line cap | 4 new extension files: `AmortizationInputsScreen+Helpers`, `SavedScenariosScreen+EditMode`, `TCAScreen+Actions`, `IncomeQualScreen+Reserves`. Same pattern as 5E.5's `TCAInputsScreen+DebtsAndLTV`. | Every time a feature lands on an Inputs/Results screen the struct creeps toward the cap. The fix is always the same — pull helpers into an extension file and relax `private` → internal on state the extension needs. Cheap structural move that preserves logical cohesion. |
| 2026-04-19 | Self-Employment scope subset (Session 5G.1) | nothing modeled | v1 ships Schedule C + Form 1120S K-1 + Form 1065 K-1 with 2-year averaging and Fannie B3-3.6-07 eligibility gating. Schedule F, Schedule E rental, Form 1120 C-corp, liquidity analysis, and COVID-era Lender Letter guidance deferred to Session 7+. | These three business types cover the DTC-mortgage self-employed borrower population v1 needs to serve. The deferrals either require counsel-reviewed forms (F, E rental, 1120), or are programmatic follow-ons (liquidity, Lender Letter). Ship the subset that lets an LO qualify most self-employed prospects. |
| 2026-04-19 | B3-3.6-07 kickout implementation (Session 5G.1) | eligibility rule not modeled | In cashFlowForm1120S and cashFlowForm1065, when ownership < 25% AND `hasConsistentDistributionHistory == false`, the pass-through income contribution collapses to 0. W-2 wages (1120S) and guaranteed payments (1065) still count because they're contractual / distribution-independent. | Fannie's B3-3.6-07 gate is the single most common reason LOs get tripped up on K-1 analysis — worth codifying in the engine so the UI can't silently count ineligible pass-through. |
| 2026-04-19 | IncomeTrend thresholds (Session 5G.1) | not modeled | ±5% → stable; > +5% → increasing; -5% to -20% → declining; < -20% → significantDecline. Declining and significantDecline both use the lower year (y2) for qualifying annual; stable and increasing use the mean. | Matches Fannie Form 1084's "use the lower year when income is trending down" rule without requiring the LO to make the classification call themselves. The -20% breakpoint is Fannie's threshold for requiring a written borrower explanation. |
| 2026-04-19 | SE import as sheet (Session 5G.6) | not available | Below the IncomeQual Gross monthly income field, a pill opens SelfEmploymentInputsScreen inside a NavigationStack sheet. On "Use this income" the qualifying monthly replaces incomes[0] with an IncomeSource kind=.selfEmployed labeled "Self-employment analysis." | Self-employed qualifying is the single most common context where LOs need this calculator. Lifting SE into a sheet-level import keeps the LO inside their qualifying flow; the alternative (navigate away, compute, remember the number, navigate back, type it in) is friction that loses context. |
| 2026-04-19 | SE Results dock polymorphism (Session 5G.4) | n/a | SelfEmploymentResultsScreen's dock uses the standard Calculator dock when invoked standalone and swaps to Cancel / "Use this income" when invoked from IncomeQual (onImportMonthly non-nil). | One screen, two call-sites, different terminal actions. A role parameter would have forced every calculator into a pattern they don't need; a closure parameter is a lightweight adapter that keeps the standalone case unchanged. |
| 2026-04-19 | SE sheet primary actions location (Session 5H.1c) | Cancel + "Use this income" rendered in a bottom safe-area dock | moved to the navigation bar — leading Cancel, trailing "Use this income" once Results is reached; title reads "Self-Employment Income" | Sheet-mode calculators should look like sheets, not like the full-screen calculator. Nav bar actions are the iOS-native idiom for "apply / dismiss" on an inline sheet; bottom dock made the imported SE calculator feel like a navigation push instead of a modal decision. |
| 2026-04-19 | SE addback UX clarity (Session 5H.1b) | flat addback/deduction list + unlabeled fields | every addback input gets an "added back" hint + subtraction inputs get a "subtracted" hint. Info icon above the first addback in each Year-1 card opens a Selling Guide B3-3.6-03 popover. Expanded Results breakdown is a signed line-item math trace — base + addbacks (+) + subtractions (−) + ownership × share step (K-1) + total. | The 5G math itself is correct — the bug was cognitive. LOs not familiar with 1084 saw "Depreciation +$8,000" on a Results page and worried we were inflating income. Surfacing the addback semantics + the math trace inline is the fix. Math unchanged. |
| 2026-04-19 | TCA scenario count selector (Session 5H.4) | fixed 4-scenario preset | 2/3/4 segmented selector (default 2) mirroring Refi 5F.3. `scenarioCount` persists per saved scenario. `resizeScenarios(to:)` pairs with `blankScenario(label:)` (term=30, others 0). | Two is the minimum for comparison; most LOs don't need four. Matches Refi's pattern so LOs learn the idiom once. Blank-slate default (0s with term=30) echoes Refi + HELOC. |
| 2026-04-19 | Blank-slate fresh-launch defaults (Sessions 5F.3 / 5H.4 / 5H.5) | pre-filled demo-looking sample values | every calculator's fresh-launch `defaultInputs` zeros out every numeric field except term-like fields which default to 30 yr | Demo data in production confuses LOs and invites data-entry errors (forgetting to override a sample). Zero-state is honest and forces explicit entry. Saved scenarios bypass fresh-launch defaults and round-trip their real values. |
| 2026-04-19 | TCA column alignment (Session 5H.2) | top scenario cards and horizon matrix columns used independent column layouts | top cards wrapped in an HStack with a 52-pt leading gutter matching the matrix's horizon-label column. Both rely on equal-width flex columns so they align at any scenario count and any device width. | The eye needs the columns to line up for a cross-reference page to read cleanly. A shared `Grid` would be cleaner long-term but would require rewriting both sections; the gutter approach lands the alignment in one commit without touching layout primitives. |
| 2026-04-19 | Palette alignment to NestIQ spec (Session 5I.4.f) | Session 2 ledger-green palette `#1F4D3F` / `#FAF9F5` / `#17160F` / `#DFE6E0` / `#85816F` | identical values — palette was already at NestIQ spec | Session 2's ledger-green decision (DECISIONS.md 2026-04-17 "Design tokens — README wins") matches the NestIQ brand pack's palette exactly. All six core NestIQ tokens (Ink #17160F, Paper #FAF9F5, Accent #1F4D3F, Accent light #DFE6E0, Accent bright / dark #4F9E7D, Muted #85816F) are already pixel-for-pixel in the Asset Catalog. No ColorSet edits needed. |
| 2026-04-19 | Brand rename Quotient → NestIQ (Session 5I.4) | App user-facing name = Quotient; bundle ID = ai.perlantir.quotient | User-facing name = NestIQ; bundle ID = mortgage.nestiq (registered in Apple Developer with Sign In with Apple + Associated Domains). Xcode target name, folder (`Quotient/`), module names (QuotientFinance / Compliance / Narration / PDF), scheme, and git repo stay "Quotient" to preserve git history. | Display-name-only rebrand keeps the diff surgical. Internal code preservation avoids a massive rename churn that would touch every import + every type reference across ~150 Swift files with no user-facing benefit. Test targets keep the `ai.perlantir` prefix (invisible to users, not worth the bundle-ID churn). |
| 2026-04-19 | SE sheet dismissal pattern (Session 5I.2) | `@Environment(\.dismiss)` on the pushed Results view | `onImportMonthly` + `onCancel` closures hoisted to the parent; SE views call those closures instead of `dismiss()`; parent flips `showingSelfEmployment = false` | A NavigationStack inside a sheet makes `dismiss()` semantics ambiguous — from a pushed view it pops nav rather than dismissing the sheet. 5H.1c's wiring left the user stranded on SE Inputs after tapping "Use this income" on Results. Closure hoisting gives the parent (the sheet presenter) unambiguous control of both paths. |
| 2026-04-19 | UI test assertion: sheet dismiss vs element existence (Session 5I.2) | `waitForExistence(incomeQual.compute)` after an action expected to dismiss a sheet | `waitForNonExistence(seSheetControl)` + assert sheet-only controls are gone | `XCUIElement.waitForExistence` returns true for elements anywhere in the view hierarchy regardless of visibility / z-order. The 5H test passed against the bug because IncomeQual's button was queryable behind the still-presented SE sheet. Future sheet-dismiss tests assert both (a) the sheet's own controls non-exist and (b) the underlying view's controls exist. |
| 2026-04-19 | Settings hero photo render (Session 5I.3) | initials-only overlay regardless of `photoData` | `profileAvatar` ViewBuilder renders `photoData` as a scaledToFill circle when present; falls back to `Image("Monogram-Accent")` otherwise | The 5B.3.b PhotosPicker + SwiftData persistence shipped correctly; the hero render path simply never consumed the field. Mirrors `ProfileEditor.photoThumbnail`'s pattern. Monogram fallback replaces the earlier initials fallback per the NestIQ placement guide. |
| 2026-04-19 | `showPhotoOnPDF` default (Session 5I.3 revisit) | default `false` | default `false` — kept | Borrowers may not want the LO's face on every borrower-facing doc; opt-in default is correct UX. When Nick's QA reported "photo doesn't show on PDF," the PDF pipeline was working as designed — the toggle just hadn't been flipped. Confirmed with Nick; no change. |
| 2026-04-19 | PDF cover branding when custom company logo is present (Session 5I.4.d) | `Text("Quotient")` fallback wordmark in the left column | `Image("Masthead-PDF")` at full width above the existing LO block, shown only when `logoData == nil` | The LO's own company logo should keep primary billing when uploaded (existing side-by-side layout). When no custom logo is set, the NestIQ masthead provides the brand presence. Two branches in `brandStrip`; the default path now carries NestIQ branding without crowding the LO's own mark in the custom-logo path. |
| 2026-04-19 | Launch screen implementation (Session 5I.4.d) | `UIColorName` only (solid accent field) | plist `UILaunchScreen` with `UIImageName=Wordmark-A` + `UIColorName=Surface` + `UIImageRespectsSafeAreaInsets` | iOS's plist launch-screen path can't express "60% viewport width"; the wordmark renders at natural size. If the 60% sizing is load-bearing we'd need a LaunchScreen.storyboard. Deferred as post-rebrand polish — natural-size + Paper background ships the NestIQ brand in the 1-tap launch experience. |
| 2026-04-19 | Imageset vector strategy (Session 5I.4.c) | n/a | Preserve Vector Data off; 1x/2x/3x PNG raster pipeline | The brand pack ships PNGs at 1x/2x/3x widths sized for iOS consumption; SVGs in the pack reference Source Serif 4 via Google Fonts (won't render correctly in `Image("...")` without a PDF-with-outlined-text conversion step). PNG path is simpler and sufficient for every wired surface. |
| 2026-04-19 | Welcome-card wordmark refresh (Session 5J.1) | serif "Q" + "QUOTIENT" + hard-coded "v 1.2.4" | `Image("Monogram-Accent")` + "NESTIQ" + Bundle-derived version+build string | Every literal remnant from the pre-5I Quotient branding got cleaned up; version now reflects the actual archive so TestFlight / App Store builds self-identify. `WelcomeMiniature` reads `CFBundleShortVersionString` + `CFBundleVersion` at render time — zero static assumptions about release cadence. |
| 2026-04-19 | IncomeQual reserves range extension (Session 5J.2) | 0-12 month stepper | 0-36 month stepper with year-label collapse at 12 / 24 / 36 | Jumbo, investor-property, and self-employed programs commonly require 18-36 months of reserves; capping at 12 forced LOs to fake the input. Year-label collapse at the round pivots is a typographic cue — "2 years" reads as an intentional program tier; "14 months" reads as a case-by-case override. JSON decode now clamps legacy Int/Double encodings to 0-36 so older scenarios round-trip cleanly. |
| 2026-04-19 | Save-scenario name prompt (Session 5J.3) | silent persist with name = borrower.fullName ?? "{Calculator type}" | single-field `.alert` with default `"{Borrower full name} · {Calculator}"` (or `"New scenario · {Calculator}"` when no borrower); edit-before-save; 60-char cap; empty at confirm → default | Generic calculator-type names made the Saved tab unusable past 3-4 scenarios per borrower. Prompt-on-save gives the LO meaningful file names while preserving the one-tap default ("{Borrower} · {Calc}") for the common case. Shared `SaveScenarioNameAlert` ViewModifier + `SaveScenarioDefaults` enum keep every calculator's default-name format identical; 6 screens wire up through the same surface. |
| 2026-04-19 | Save-scenario name prompt — "Run scenario" exception (Session 5J.3) | Run-scenario inherited the Save prompt | Run-scenario from IncomeQual silently persists with default name, then jumps to Amortization | The prompt intercepts explicit Save taps; Run-scenario is a "save + navigate" compound action where a prompt would block the expected screen transition. Silent persist preserves the pre-5J Run-scenario UX; the Save button on either side (IncomeQual or Amortization) still opens the prompt. |
| 2026-04-19 | UI tests confirm Save alert (Session 5J.3 follow-up) | tests tapped `dock.save` and immediately asserted row appearance | tests tap `dock.save`, then `UITest.confirmSaveAlert(app)` which taps the alert's Save button | The 5J.3 prompt blocks silent save, so every save-then-query test (ScenarioSaveLoad × 5, SavedScenariosDelete seed helpers × 3, CalculatorAmortTests, CalculatorSelfEmploymentTests × 3) had to accept the default name via the alert. Centralized as a helper so 5K+ tests see the idiom, not the raw alert query. |
| 2026-04-19 | PDF masthead transparency (Session 5J.4) | `<rect fill="#FAF9F5">` baked in at SVG-source level → cream RGBA PNG on white PDF page | threshold-masked transparent RGBA PNG: cream (#FAF9F5) pixels → alpha 0; all other pixels kept at source RGBA | The v2 brand pack didn't change the opacity of the background — both v1 and v2 exports carried the paper rect baked in. Nick's round-5 QA flagged the resulting tan box. Post-processed both @1x and @2x PNGs via a one-shot Swift tool (/tmp/masthead_transparentize.swift, not checked in). Threshold-based mask preserves the muted-gray #85816F "MORTGAGE INTELLIGENCE" label at its intended tone; simpler than per-pixel alpha unpremultiplication and visually indistinguishable at letterhead print scale. |
| 2026-04-19 | Settings footer copy (Session 5J.5) | "NestIQ — made in Portland, OR" | "NestIQ · Powered by Perlantir" | Attribution to the studio, not a specific city. Middle-dot separator matches the NestIQ eyebrow style already used across the app. |
| 2026-04-19 | Brand pack v2 asset refresh (Session 5J.7) | v1 NestIQ brand pack assets (Wordmark-A 1080/1620/3240w; Monogram 256/512/1024; AppIcon 18 scales; Masthead 1200x360) | v2 artwork dropped in: AppIcon × 18, Wordmark-A primary @1x/@2x/@3x from 480/1080/1620w exports (aspect shift 3.86 → 3.43), Wordmark-A reverse-ink @1x/@2x, Monogram-Accent @1x/@2x/@3x at matching 256/512/1024 sizes. Masthead-PDF refreshed via the 5J.4 transparentize pipeline off the v2 2400w source. | V2 ships refined glyph geometry + tighter icon proportions; commit 3601fd1 staged the pack at `brand/nestiq-logo-pack/` before this session. Wordmark aspect shift is a deliberate design refinement Nick approved pre-session. V2 doesn't ship a 3240w wordmark primary export, so @3x maps from 1620w (still 3.375× the @1x width) — Image("Wordmark-A") is always rendered at .frame(height:)-driven sizes so display pt dimensions are unchanged. |
| 2026-04-19 | Smart Save provenance tracking (Session 5K.1) | every Save tap opened the 5J.3 name-prompt alert; overwrite-in-place was a separate code branch inside each saver that the prompt unconditionally bypassed | `promptSaveScenarioName()` on each of the six calculator results screens branches on `existingScenario`: when set, the overwrite branch runs with the existing record's name and no alert; when nil, the 5J.3 prompt fires unchanged. `justSaved` dock-label pulse still confirms either path. | Provenance already lives on the screen (each calculator's Inputs/Results view takes `existingScenario: Scenario?`) — no shared/global scenario-tracking layer required. The fix is six identical 4-line edits, not a new architecture. Loading a saved scenario and hitting Save should not produce a duplicate; the prompt should only gate the fresh-to-named transition. |
| 2026-04-19 | Narration flagged-numbers extractor (Session 5K.2) | regex `\$?\d{1,3}(?:,\d{3})*(?:\.\d+)?%?` that stopped at any alphabetic suffix; exact-string allowlist match only | regex extended to `\$?\d{1,3}(?:,\d{3})*(?:\.\d+)?[KMB]?%?` so compact-currency tokens extract whole (`$732K`, `$1.24M`, `$2B`); a ±1% normalized-numeric fallback runs past the exact match — both sides are parsed into Double (stripping $/,/%, honoring K/M/B multipliers) and a token is considered known if any allowlist entry's value is within 1%. Short-fragment skip rule treats K/M/B as context sigils alongside $/%. | Root cause was copy rendering `$732K` (dollarsShort output) while the extractor returned `$732` and missed the `$732K` allowlist entry — a hallucination warning on a validated number undermines the entire guard. Tolerance absorbs the rounding delta between the compact display form in copy and the precise form in the allowlist (e.g. `$732K` copy vs `$732,456` allowlist — 0.062% apart, now matches). |
| 2026-04-19 | Welcome-screen top identity anchor (Session 5K.3) | 48pt `Wordmark-A` centered above the WELCOME eyebrow, 24pt gap to card; 5J.1 removed the redundant H1 but left the full wordmark at the top | 40pt `Monogram-Accent` centered above the eyebrow with 24pt breathing room; the WelcomeMiniature card below continues to carry the wordmark + version lockup | A wordmark at the top plus the same wordmark in the card below read as two mastheads competing for attention. A single smaller monogram at the top anchors the screen without duplicating the card's lockup; matches Nick's round-6 QA intent ("small identity anchor so users know what app they're in"). |
| 2026-04-19 | Recent-scenario dispatch reuse (Session 5K.4) | HomeScreen's recent rows were passive cards; SavedScenariosScreen held `openScenarioDestination(_ s: Scenario)` as a private @ViewBuilder wiring six case branches | extracted the dispatcher to a standalone `ScenarioDestinationView(scenario:)` used by both screens; HomeScreen's recent rows become Buttons that set `$openScenario`, driving a new `navigationDestination(item:)`; each row gains a trailing chevron.right affordance + `home.recent.row.{type}` AX identifier | Saved tab and Home recent list should behave identically — same decoder, same `existingScenario` handle feeding into 5K.1's smart-save overwrite, same deep-link semantics. Keeping the dispatcher inline in SavedScenariosScreen would have forced a copy-paste that drifts over time; extracting to a shared view file pins the contract in one place. The chevron affordance matches every other tappable row in the app (Saved, Settings nav items). |
| 2026-04-19 | Reverse-ink wordmark transparency (Session 5L.1) | v2 brand pack shipped `wordmark-a-reverse-ink` with ink `#17160F` painted as opaque background (~87% of pixel area); dark-mode Settings About rendered it as a visible ink rectangle against dark surfaces | threshold-masked transparent RGBA PNGs at @1x + @2x: ink pixels → alpha 0, cream glyph pixels (250,249,245) kept at original opacity, edges within `[10..50]` channel-distance of ink get proportional alpha fade. Regenerated via `/tmp` PIL one-shot (not committed). Primary wordmark was already transparent; Monogram-Accent is a full-bleed tile by design and unchanged; Masthead-PDF already done in 5J.4. | Same State-A failure mode 5J.4 solved for the PDF masthead; same fix pattern carried forward. Asset-level fix is sufficient because none of the consuming surfaces (Settings About, Home 5L.2, Welcome 5L.3) wrap `Image("Wordmark-A")` in a `.background()` modifier. |
| 2026-04-19 | Home screen brand mark (Session 5L.2) | no brand mark anywhere on Home; upper-right initials circle is the profile button, not a brand identity | 24pt `Image("Wordmark-A")` centered above the date eyebrow + greeting, with a 12pt top gutter and 20pt total gap to the eyebrow row. AX identifier `home.brand.wordmark` + AX label "NestIQ". | Home is the flagship / most-visited surface and deserves a brand mark; Saved / Scenarios / Settings stay brand-quiet so attention isn't diluted. Relies on 5L.1's asset-level transparency fix for clean blending in both appearance modes. |
| 2026-04-19 | Welcome-screen brand mark (Session 5L.3, reverts 5K.3) | 5K.3 replaced the 48pt `Wordmark-A` above the WELCOME eyebrow with a 40pt `Monogram-Accent` on the premise that the wordmark duplicated the lockup inside the WelcomeMiniature card below | restored to 48pt `Wordmark-A`; WelcomeMiniature card still carries its wordmark + version lockup below | Round-7 QA review: the monogram read as too quiet for the app's first user-visible surface. The full wordmark is the correct mark for the welcome step even with the redundant mark inside the card — welcome earns the larger brand moment. Acceptable redundancy. |
| 2026-04-19 | Licensed-states editing dual-entry (Session 5L.4) | edits only reachable via Profile → Licensed states; Per-State Disclosures preview had no edit affordance | three entry points all routing through one `LicensedStatesPickerSheet` wrapper: (1) existing ProfileEditor row, (2) new Settings → Disclaimers · compliance → Licensed states row with compact trailing preview ("IA, CA, TX · 4 states" / "None"), (3) new "Edit licensed states" button in Per-State Disclosures preview. Sheet seeds selection from `profile.licensedStates` and commits back on dismiss (+ `updatedAt`). | LOs previewing per-state disclosures frequently wanted to add / remove states without backing out to Profile. Shared picker wrapper keeps persistence semantics identical across entry points; one sheet, three surfaces. Trailing preview in Settings surfaces the current state without opening the picker. |
| 2026-04-20 | APR is display-only (Session 5M D1) | unspecified — ambiguous whether APR entries feed back into amortization or payment math | APR never drives engine math. All calculations continue to use the note rate. APR is purely a borrower-facing display field the LO enters from their LOS quote. | TILA § 1026.22 APR math is regulated territory — we're not building a compliant APR engine, we're surfacing what LOs entered for borrower-facing compliance context. Keeps the engine clean and avoids a whole class of regulatory exposure. |
| 2026-04-20 | APR display pattern (Session 5M D2) | unspecified | When APR blank or within 0.0005% of rate, display shows rate alone ("6.750%"). When APR meaningfully differs (including credits case where APR < rate), display shows both with "APR" suffix on the second ("6.750% / 6.812% APR"). | The 0.0005% tolerance absorbs sub-display-precision drift; two values that render identically (e.g. 6.7500001 vs 6.7500000) shouldn't paint a redundant "6.750% / 6.750% APR" just because their Double representations differ by epsilon. |
| 2026-04-20 | APR schema (Session 5M D3) | unspecified | `Optional<Decimal>` on each calculator's Codable FormInputs. Blank LO entry stores `nil`; display treats `nil` as "same as rate." JSON round-trips cleanly via `decodeIfPresent(...)`. | Consistent with the Decimal-for-money convention (even though `asDouble` is used at display time); matches the existing `monthlyMI`, `currentAPR`, etc. nullability patterns. |
| 2026-04-20 | Unrecoverable costs definition (Session 5M D4) | unspecified | Interest + Mortgage Insurance + Closing Costs. Taxes + insurance + HOA render separately on an "Ongoing housing costs (paid regardless)" line. PDF glossary footnote explains the distinction so LOs can speak to it. | Unrecoverable = doesn't build equity / doesn't transfer to the borrower. Tax/ins/HOA apply whether owning OR renting, so they're not a cost of ownership; they're a cost of housing-the-property. Keeps the apples-to-apples comparison honest. |
| 2026-04-20 | Break-even format (Session 5M D5) | unspecified | "Month 34 (~2.8 years)" on-screen + Swift Charts crossover visualization. PDF ships a compact one-line per-scenario summary (full chart deferred — comparison PDF page at density ceiling). Refinance-mode only; baseline is scenario index 0; formula is cumulative savings == scenario.closingCosts. | Integer-month label with fractional-year gloss matches LO verbal framing. Crossover chart (LineMark = cumulative savings; RuleMark dashed = closing-cost ref line) is the signature refi visualization for borrower conversations. |
| 2026-04-20 | Reinvestment defaults (Session 5M D6) | unspecified | 7% annualized (conservative S&P long-run average), LO-editable per TCA analysis. `TCAFormInputs.reinvestmentRate: Decimal` persists the rate; `TCAFormInputs.defaultReinvestmentRate` is String-initialized from "0.07" (routing through Double introduces 0.07000000000000001024 binary drift otherwise). Disclaimer copy required on every rendering surface. | S&P long-run conservative bracket; 5M's first field in QuotientFinance to surface an illustrative-return math path that warrants inline disclaimer, modeled after the Session 2 EHO / disclaimer library convention. Past performance / market risk wording matches SEC-compliant investor-education boilerplate. |
| 2026-04-20 | Scenario schema migration strategy (Session 5M D7, corrected) | spec called it "SwiftData migration" | Scenario payloads live in `Scenario.inputsJSON: Data` — JSON-blob Codable, not SwiftData field-level schema. New-field migration is Codable-level: `decodeIfPresent(...) ?? default`. No SwiftData version bump. `SchemaMigrationTests.swift` confirms every pre-5M FormInputs JSON decodes cleanly into the 5M Codable with new fields at sentinel defaults. | D7 as originally written described a SwiftData migration that would have been wrong — the per-calculator payload is JSON, not a SwiftData relation. 5E.5's `includeDebts` + 5J.2's `reservesMonths` (0-36 extension) already set the additive-codable precedent; 5M just extends the pattern. |
| 2026-04-20 | Delete account (Session 5N.1) — SIWA revocation path | Apple Guideline 5.1.1(v) requires in-app deletion; SIWA `POST /auth/revoke` needs a client-secret JWT signed with the team's p8 key | Local-only v1: `AccountDeletion.performLocalWipe(context:preservingProfile:)` clears all SwiftData (Scenario → Borrower → LenderProfile) row-by-row; success screen tells the user "Your Apple ID association will be fully cleared by our systems within 7 days"; `// TODO(server):` inline marks the future revocation endpoint. Keychain + UserDefaults scopes audited empty (no NestIQ-prefixed keys). Two-step confirm sheet with destructive final Delete. | The p8 private key cannot ship in a mobile binary — extraction is trivial with class-dump. A server endpoint that holds the key and signs JWTs on the app's behalf is the GA path but out of scope for this session. Apple's guideline permits delayed revocation when the user is informed up front, which the 7-day copy handles for TestFlight. |
| 2026-04-20 | PDF page header pattern (Session 5N.2) | cover used Masthead-PDF image + custom signature strip; disclaimers ran a separate small Wordmark-A + hardcoded "Page 2 of 2"; comparison / schedule pages each rolled their own chrome (Nick's QA caught "tIQ Mortgage Intelligence" clipped on page 2) | Shared `PDFPageHeader` at the top of every PDF page: centered `Image("Wordmark-A")` @18pt, SF Mono "Page N of M" / long-form date row, 0.5pt muted divider, 24pt space to content. Global page count computed once in `PDFBuilder.buildPDF` (cover=1, extras=2..N-1, disclaimers=N) and threaded into each page view's `pageIndex` / `pageCount` params. Schedule pages use `AmortizationSchedulePages.pageCount(schedule:granularity:)` to pre-compute their slice count so the global total is accurate before any page is constructed. | One component, one call site, one formatDate helper — removes an entire class of "one page header drifts" regressions. Each page keeps its calculator-specific title band below the global header so context isn't lost. Landscape pages (schedule + comparison) share the same pattern; text-extraction on rotated pages is less reliable, so page-counter tests anchor on cover + disclaimers only. |
| 2026-04-20 | PDF signature block root cause + fix (Session 5N.3) | Nick's QA: two separate name blocks on the PDF cover ("Nick Debug / Senior Loan Officer / NMLS … / company / email / phone" + "Nick Gallick / Gallick Holdings LLC") | Root cause: `LenderProfile.tagline` was passed into `PDFCoverPage.signatureLine` and rendered as a second italic serif block below the main signature. Fix: removed the `tagline` field from the model, deleted the `SignatureBlockEditor` from Settings → Brand, removed `signatureLine` param + render from `PDFCoverPage`. Rebuilt signature: serif 16pt name / title · NMLS / company (own line, collapses when empty or "—") / email · phone joined by middot / 56pt circular photo on the right. Single LenderProfile record drives the entire block — no separate `CompanyProfile` exists. | `tagline` was an editable free-form field intended as "a short line beneath your name and NMLS" — LOs mistook it for a contact-info slot and pasted full signatures into it. Killing the field is simpler than trying to rein in its use, and the core signature block already carries every field LOs actually need. |
| 2026-04-20 | Break-even chart redesign (Session 5N.4) | Chart rendered negative cumulative savings (scenarios that cost more than baseline dipped to -$100K); no crossover marker; dashed refs matched scenario color so the "reference" visual weight was indistinguishable from the signal | Y-axis clamped to non-negative (`chartYScale(domain: 0...yMax)`); scenarios with `payment ≥ baseline` excluded from the chart (handled by 5N.5 reinvestment messaging instead); dashed `RuleMark` rendered in neutral `Palette.inkTertiary` with "Closing $X" trailing annotation; `PointMark` at each scenario's break-even month with "Break-even · Month N" label. Per-scenario description paragraph below chart ("savings exceed closing costs at month 34 (~2.8 yr) — net positive thereafter" / "savings do not exceed closing costs within the 30-yr term"). | Negative y-axis was technically correct (cumulative savings can go negative when payment > baseline) but confusing for borrower conversations; excluding those scenarios from the chart and routing them to 5N.5's explainer messaging is clearer. PDF chart rendering deferred to a fast 5O follow-up per Nick's 5N.4 time-box decision — on-screen chart shipped alone, PDF keeps the compact text summary. |
| 2026-04-20 | Reinvestment messaging for non-positive savings (Session 5N.5) | Scenarios with payment ≥ baseline showed "Invest 5yr $0 · 7yr $0 · 10yr $0 · 15yr $0 · 30yr $0" with no explanation — math was correct (no savings to invest) but display was confusing | Three-way classification on `baseline − scenarioPayment`: **positive** ($0.01+ diff) → normal horizon projections; **zero** (|diff| < $0.01) → "Equivalent monthly payment — no savings to reinvest."; **negative** → "[LABEL] costs $X/mo more per month than baseline — no monthly savings available to invest." Penny epsilon absorbs same-P&I-to-the-cent round-trips. Applied to both on-screen `TCAScreen+BreakdownSections` and PDF `TCAComparisonPage+Helpers` compact summary. Scenario header + colored dot stay visible so users know the section was considered. | Silent $0 numbers read like a broken calculator; an explainer reframes the (correct) math as a deliberate "no savings here" call. Engine primitives (`pathAInvestmentBalance`, `pathBExtraPrincipal`) unchanged — this is purely a display-layer classification. |
| 2026-04-20 | PDF rendering architecture (Session 5O D8) | `ImageRenderer` + `CGContext.pdfContext` composing fixed-size SwiftUI views per page (612×792 portrait, 792×612 landscape). PDF chrome (header, page counter, disclaimers) duplicated across calculator files; repeated multi-page pagination failures across 5J / 5M / 5N; ~2,870 LOC across 14 SwiftUI files | HTML-to-PDF via `UIPrintPageRenderer` + `WKWebView.viewPrintFormatter()`. Body composition as mustache-style HTML (`App/Resources/PDFTemplates/base.html` + `SignatureBlock.html` interpolated by per-calc `*PDFHTML.swift` modules). Per-page NestIQ wordmark header + "Page N of M · nestiq.mortgage" footer drawn in Core Graphics by `NestIQPrintRenderer` (`drawHeaderForPage` / `drawFooterForPage`). Charts render as inline SVG (`BreakEvenChartSVG`). Six per-calculator builders now route through `HTMLPDFRenderer.renderPDF(html:to:)` which is `async throws`. | Pivoted from the original plan's `WKWebView.createPDF` + CSS `@page` running elements after confirming WebKit does not implement CSS Paged Media Level 3 (no `position: running()`, no `counter(page)`). `UIPrintPageRenderer` is the canonical iOS paginated-print pipeline (Encompass, Blend). WKWebView paginates the body; Core Graphics owns the chrome. Fonts use Georgia cascade fallback to Source Serif 4 (not a system font on iOS). Legacy SwiftUI PDF module deleted in 5O.8; `QuotientPDF` package now exposes only `PDFInspector`. |
| 2026-04-20 | Break-even chart x-axis domain (Session 5O.9) | Chart used Swift Charts' auto-computed x-axis domain with a 360-month global data cap; for tight data ranges, Charts expanded the visible domain out to ±500 months (Nick's 5N QA bug) | Chart x-axis hard-clamped to the longest included scenario's term via `.chartXScale(domain: 0...xMax)`. Scenarios that don't cross closing costs within their own term are excluded from `breakEvenSeries` entirely (no flat line below the reference); their description line ("savings do not exceed closing costs within the N-yr term") still renders. When no scenarios cross, the Chart itself is hidden via `if !series.isEmpty`. PDF side: `BreakEvenChartSVG` hard-codes its viewBox to termMonths as a matching constraint. | Auto-domain misbehavior was unpredictable — data magnitudes interact with Swift Charts' internal padding heuristics. Hard-coding to the scenario term is the correct ceiling: you can't break even AFTER the loan is paid off. Excluding non-crossing scenarios from the visual (but keeping them in the descriptive text) matches the chart redesign's intent — the chart exists to show crossovers, not to plot every non-winning path. |
| 2026-04-20 | Current mortgage on Borrower (Session 5P D9) | Refinance-mode calculators (TCA refi, Refinance Comparison, HELOC vs Refi) asked the LO to re-type the borrower's current balance / rate / P&I / home value on every scenario. TCA break-even further used scenario A as the baseline ("scenario-vs-scenario") which said "Never" even when a proposed refi clearly saved the borrower money vs their existing loan. | Current mortgage lives on `Borrower` as `currentMortgageJSON: Data?` with a computed `currentMortgage: CurrentMortgage?` accessor (get decodes, set encodes/clears). `CurrentMortgage` is a pure Codable/Hashable/Sendable value struct with 7 fields: currentBalance, currentRatePercent, currentMonthlyPaymentPI, originalLoanAmount, originalTermYears, loanStartDate, propertyValueToday. `CurrentMortgageCalculations` helpers in QuotientFinance (monthsPaid / monthsRemaining / ltvToday / equityToday) operate on primitives. Scenarios snapshot the mortgage in their inputsJSON so loading an old scenario restores the baseline. TCA refi break-even now anchors on `currentMortgage.currentMonthlyPaymentPI` (all scenarios become candidates); term-clamp uses `monthsRemaining` so a proposed refi only has the remaining life of the old loan to recoup closing. Refinance Comparison + HELOC prefill their existing current-loan fields from the attached borrower. | JSON blob over native SwiftData because SwiftData's Objective-C bridge has misfired on non-primitive attributes in this project (5E.1 `[String]` incident, 5N.1 `@Model` wiring). The `licensedStatesCSV` / `licensedStates` pattern at `App/Storage/Models.swift:98-106, :179-187` is the working precedent. JSON blob keeps the Codable value type clean for calculators to pass around without introducing `@Relationship` semantics, inverse rules, or cascade delete edge cases. The "partial drafts stay local; only valid OR blank commits" rule on `CurrentMortgageDraft` keeps mid-edit state from corrupting break-even math downstream. |
| 2026-04-20 | Borrower→Scenario delete rule (Session 5Q.2 D10) | `@Relationship(deleteRule: .cascade, inverse: \Scenario.borrower)` on `Borrower.scenarios` — deleting a borrower cascade-deleted every scenario that referenced them. Introduced when `Scenario.borrower` first shipped; never revisited. Conflicted with 5Q.2's "LO can delete stale borrower records without forfeiting analysis work" behavior. | Flipped to `.nullify`. When a borrower is deleted, their scenarios survive with `scenario.borrower == nil`. The scenario's own `inputsJSON` / `outputsJSON` + the 5P.8 `currentMortgage` snapshot preserve correctness without the live borrower link. All existing UI / PDF surfaces already tolerate a nil borrower (calculator Results screens use `viewModel.borrower?.fullName ?? "Untitled scenario"`; SavedScenariosScreen + HomeScreen rows use `scenario.borrower?.fullName ?? scenario.name`; PDFs fall back to "Client"). No schema migration — `Scenario.borrower: Borrower?` was already optional. | Scenarios are time-sensitive historical artifacts with legal / audit value (what the LO quoted to whom, when). Deleting a contact should not destroy evidence of work done on their behalf. LOs cleaning up outdated borrower records need a safe path that doesn't take scenarios with it — cascade was the wrong default for a mortgage professional's workflow. Nullify matches the pattern Apple ships in Contacts/Mail/Notes where deletions are additive removals, not tree-destroying mass clears. |
| 2026-04-20 | Saved-scenario detail edit path (Session 5R.6 D11) | Tapping a saved Refinance / IncomeQual / TCA / HELOC scenario routed via `ScenarioDestinationView` straight to the Results screen with no affordance to return to Inputs for modification. (Amortization + Self-Employment already routed to their Inputs screens.) LOs had to duplicate + recompute, or back out to a fresh calculator flow, to change any value on a saved scenario. | Kept the existing Results-first routing (one-tap to the computed output for these four calculators) and added a conditional "Edit" trailing toolbar button that renders only when `existingScenario != nil`. Tapping Edit pushes the corresponding `<Name>InputsScreen` with the current viewModel inputs and the `existingScenario` forwarded so 5K.1 smart-save still overwrites in-place after editing. Required a new `initialInputs: <Name>FormInputs?` parameter on all four Inputs screens; when present, it wins over `applyBorrowerCurrentMortgage` so loaded scenario values aren't clobbered by borrower defaults. `RefinanceScreen` + `IncomeQualScreen` toolbars extracted to `@ToolbarContentBuilder` extension properties to stay under SwiftLint's `type_body_length` cap. AX identifiers: `refinance.edit` / `incomeQual.edit` / `tca.edit` / `heloc.edit`. | Considered normalizing all six calculators to Inputs-first routing (the Amortization / Self-Employment pattern) but that regresses saved-scenario tap behavior — users who expect to land on Results for the four affected calculators would pay an extra Calculate tap on every saved-scenario open. Conditional Edit button is additive, preserves the established UX, and matches how Apple surfaces edit-in-place on read-first detail screens (Notes, Reminders). Gating on `existingScenario != nil` keeps fresh-flow Results screens uncluttered — the back-chevron to the Inputs screen below already handles the create path. |
