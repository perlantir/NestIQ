// QuotientNarration
//
// Placeholder for Session 1. Full implementation lands in Session 4:
// - FoundationModels wrapper (iOS 18.2+, on-device LLM)
// - Template-based fallback for unsupported devices
// - EN + ES variants per calculator type
// - Capability detection + silent auto-selection
// - Number-allowlist regex post-processing for hallucination safety
// - Streaming token-by-token API via AsyncThrowingStream

import Foundation

/// Intentionally empty for Session 1; here only so the target compiles.
public enum QuotientNarration {
    public static let placeholder = "session-4"
}
