# V0.1.2 Backlog

Work deferred from v0.1.1 (TestFlight build 3). Each entry names the session/commit where the deferral was made and the specific v0.1.2 scope.

## PDF template — finish remaining refi tokenization

Deferred from **Session 7.2b** (2026-04-21). Refi PDF template (`pdf-refinance-with-masthead.html`) was partially tokenized with 48 per-cell tokens covering the side-by-side matrix, page 1 break-even hero, and page 3 assumptions box. Three literal regions remain:

1. **SVG break-even chart (page 1, L.215-269)** — path `d=` coordinates, endpoint labels (`A · +$42K`, `B · +$68K`, `C · 15yr`), axis labels, break-even marker position, and chart legend ("Option A · 30-yr 6.375%" etc.). Requires Swift-side SVG path math + label composition.
2. **Analyst narrative block (page 2, L.429-457)** — retains `{{recommended_option_tag}}`, `{{recommended_closing_costs_formatted}}`, `{{prepared_by_name}}`, `{{prepared_date}}` but leaves inline dollar/percent figures (`$285`, `9-year`, `50 bps`, `18 months`, `28% DTI`) as literal demo values.
3. **Savings-by-holding-period grid (page 3, L.503-526)** — 5 cells (`+$2,220`, `+$9,060`, `+$22,740`, `+$41,300`, `+$68,400`) plus "recouped + 8 mo" label and "9 yr · planned" horizon label.

Estimated token additions for full tokenization: ~15-20 tokens (SVG) + ~10 tokens (narrative) + 7 tokens (savings grid) = ~35 more. Bumps refi total past 80 tokens — may warrant a schema restructure (e.g., grouping into `option_*`, `current_*`, `narrative_*`, `savings_horizon_*` namespaces).

Scope kept tight in v0.1.1 to ship TestFlight on time. See Session 7 commit history for the 49 tokens shipped (48 approved + `recommended_breakeven_months_short` added to replace literal "28" in the page 1 break-even hero).

## PDF template — dynamic gain/loss color classes on refi comparison rows

Deferred from **Session 7.2b** (2026-04-21). Two rows in `pdf-refinance-with-masthead.html` (page 2 comparison matrix) have CSS classes (`gain`/`loss`) that were left hardcoded to the demo configuration:

1. **"vs current" delta row** (`rc-cell gain / gain / loss` for options A / B / C) — assumes options A + B lower the monthly payment and option C raises it (typical when C is a shorter-term product). If a borrower's quote set violates that pattern (e.g., option A raises payment), the green/red coloring will be wrong.
2. **"Lifetime net savings" row** (`rc-cell gain` on all three options) — assumes every option produces positive lifetime savings vs current. If any option has negative lifetime savings, the green coloring will be wrong.

Fix shape: either introduce 6 class tokens (`option_{a,b,c}_pi_delta_class`, `option_{a,b,c}_lifetime_savings_class`) or replace each row with a single sentinel (e.g., `<!--{{pi_delta_row}}-->`) and have the Swift builder emit sign-aware cell HTML. Sentinel approach keeps token count down but shifts more structure to Swift.

Low impact for v0.1.1 since demo-typical quote sets match hardcoded classes, but should be fixed before public App Store release.

## FRED Prime rate wiring

Deferred from **Session 7.3b** (2026-04-21). HELOC calculators added `helocIndexType` (`.prime` / `.wsjPrime` / `.sofr`) + `helocMarginPct` input fields to back the v2.1.1 PDF's `{{heloc_index_plus_margin_pct}}` token. The PDF renders "Prime + 0.50" etc. today from those two fields alone; there is no live Prime rate source in the app.

For the full experience (show the LO "today's Prime = 8.50%" so they can sanity-check their margin spread before picking a product), wire `FREDRateService` to fetch the `DPRIME` series (Daily Bank Prime Loan Rate, https://fred.stlouisfed.org/series/DPRIME) alongside the existing PMMS 30-year and 15-year series. Surface on the Home rates widget and the HELOC Inputs "Advanced" section.

Hardcoded fallback assumption as of Apr 2026: Prime = 8.50%. Update the hardcoded default and the sample seed when the FRED wiring lands.

## TCA PDF — migrate to v2.2 template

Deferred from **Session 7.3** (2026-04-21). The v2.1.1 TCA with-masthead template has no page real estate for three sections currently shipped in the Swift-built TCA PDF:

1. Interest-vs-principal breakdown (per scenario)
2. Unrecoverable cost analysis
3. Reinvestment strategy section (restored in Session 5Q.6)

Session 7.3 keeps TCA on its existing Swift-built HTML path (`TCAPDFHTML.swift` untouched; still uses `PDFHTMLComposition.wrap(body:)` + `base.html`). The v2.1.1 TCA template (`pdf-tca-with-masthead.html` with `{{matrix_rows}}` sentinel) is bundled at `App/Resources/PDFTemplates/templates/` but unused at runtime.

Unblocks when Claude Design ships a v2.2 TCA template that accommodates the three sections. Coordinate with design before migrating.

## Narrative slots for Amort + HELOC + TCA templates

Deferred from **Session 7.3** (2026-04-21). AI-generated narrative is currently injected into the refi template's existing `<div class="narrative">` via a new `<!--{{narrative_body}}-->` sentinel. The v2.1.1 Amortization and HELOC with-masthead templates have no narrative section — Session 7.3 drops the `narrative: String` parameter from `PDFBuilder.buildAmortizationPDF` and `buildHelocPDF`; FoundationModels-generated narrative is simply not rendered in those two PDFs for v0.1.1.

v0.1.2 tasks:

- Coordinate with Claude Design on narrative block designs for `pdf-amortization-with-masthead.html` and `pdf-heloc-with-masthead.html` (v2.2 upstream).
- Add `<!--{{narrative_body}}-->` sentinel at the agreed location in each.
- Restore `narrative: String` parameter on the two PDFBuilder functions; wire AI narrative through.
- TCA narrative slot designed alongside the TCA template migration above.

## Trailing disclosures page — migrate to designed template

Deferred from **Session 7.3** (2026-04-21). Per D12-C2, the per-licensed-state compliance disclosures from `PDFHTMLComposition.disclaimersHTML()` are appended as a hand-styled trailing `<div class="page">` after the v2.1.1 template's editorial pages. The styling lives in `App/Features/Share/PDFTemplateLoader.swift` (or wherever the trailer is built in 7.3) — not in the v2.1.1 template package.

When Claude Design ships a v2.2 disclosures page template (e.g., `pdf-disclosures.html` with tokens for state list, NMLS footer, EHO marks), migrate the Swift trailer to consume that template instead of generating the page inline.

## Legacy builder path deletion (post-v0.1.1 ship)

Deferred from **Session 7.3** (2026-04-21). Once v0.1.1 ships to TestFlight and any feedback-driven fixes land, audit and delete:

- `PDFHTMLComposition.wrap(body:)` — only used by TCA in v0.1.1; delete when TCA migrates to v2.2 template.
- `PDFHTMLComposition.disclaimersHTML(...)` — subsumed into the new trailer helper; may be callable at a dedicated compliance module instead.
- `App/Resources/PDFTemplates/base.html` + `SignatureBlock.html` — obsolete once TCA migrates; delete.

Grep for orphan usage before deleting; watch for test fixtures, preview code, or incomplete-replacement references. Any surviving calls trace back to TCA's pre-migration path and should be migrated first.
